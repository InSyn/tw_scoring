# 🎤 Установка расширения Speech to Cursor

## 📍 Расположение расширения

Расширение находится в папке: `speech-to-cursor-extension/`

## 🚀 Быстрая установка (рекомендуется)

### Вариант 1: Режим разработки (для тестирования)

1. **Откройте папку расширения в Cursor:**
   ```
   File → Open Folder → speech-to-cursor-extension
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите расширение:**
   - Нажмите `F5` в Cursor
   - Откроется новое окно "Extension Development Host"
   - В новом окне расширение будет активно!

### Вариант 2: Установка как расширение

1. **Соберите расширение:**
   ```bash
   cd speech-to-cursor-extension
   npm install -g @vscode/vsce
   vsce package
   ```
   Это создаст файл `speech-to-cursor-0.0.1.vsix`

2. **Установите в Cursor:**
   - `Ctrl+Shift+P` (или `Cmd+Shift+P` на macOS)
   - Введите: `Extensions: Install from VSIX...`
   - Выберите созданный `.vsix` файл

3. **Перезапустите Cursor**

## ✅ Проверка установки

После установки вы должны увидеть:

1. **В строке состояния (Status Bar)** внизу окна:
   - 🎤 `$(mic) Speech - Ready to record`

2. **В списке расширений:**
   - `Ctrl+Shift+X` → найдите "Speech to Cursor"
   - Должно быть установлено и активировано

## ⚙️ Первая настройка

1. **Откройте настройки:**
   - `Ctrl+,` (или `Cmd+,` на macOS)

2. **Найдите настройки расширения:**
   - В поиске введите: `speechToCursor`

3. **Настройте язык (для русского):**
   - `speechToCursor.language` → установите `ru-RU`

4. **Проверьте режим вывода:**
   - `speechToCursor.outputMode` → должен быть `chat`

## 🎯 Первое использование

1. **Откройте чат Cursor AI:**
   - Нажмите `Ctrl+L` (или `Cmd+L` на macOS)

2. **Начните запись:**
   - Кликните на статус в строке состояния: 🎤 `Speech - Ready to record`
   - Или используйте команду: `Ctrl+Shift+P` → `Speech to Cursor: Start Recording`

3. **Говорите:**
   - Статус изменится на: 🔴 `$(record) Listening…`
   - Распознанный текст автоматически появится в поле ввода чата

4. **Остановите запись:**
   - Кликните на статус снова
   - Или `Ctrl+Shift+P` → `Speech to Cursor: Stop Recording`

## 📚 Дополнительная документация

- **Подробная инструкция:** `SPEECH_TO_CURSOR_RU.md`
- **Быстрый старт:** `speech-to-cursor-extension/QUICK_START_RU.md`
- **Инструкция по установке:** `speech-to-cursor-extension/INSTALL_RU.md`

## 🛠️ Решение проблем

### Расширение не появляется

- Перезапустите Cursor
- Проверьте, что расширение установлено: `Ctrl+Shift+X` → найдите "Speech to Cursor"

### Микрофон не работает

1. Проверьте разрешения микрофона в системе:
   - **Windows:** Настройки → Конфиденциальность → Микрофон
   - **macOS:** Системные настройки → Безопасность → Микрофон
   - **Linux:** Проверьте настройки PulseAudio

2. Попробуйте изменить движок:
   - `speechToCursor.engine` → установите `browser`

### Текст не отправляется в чат

1. Убедитесь, что чат Cursor открыт: `Ctrl+L`
2. Проверьте настройку: `speechToCursor.outputMode` → должен быть `chat`
3. Попробуйте режим clipboard:
   - `speechToCursor.outputMode` → установите `clipboard`
   - После распознавания вставьте текст вручную: `Ctrl+V`

### Плохое качество распознавания

1. Проверьте язык:
   - `speechToCursor.language` → должен соответствовать языку вашей речи
   - Для русского: `ru-RU`

2. Улучшите условия:
   - Говорите в тихом помещении
   - Говорите четко и в нормальном темпе
   - Убедитесь, что микрофон работает правильно

## 🎉 Готово!

Теперь вы можете использовать голосовой ввод для работы с Cursor AI!

**Приятной работы! 🎤✨**


