# 🎤 Инструкция по использованию расширения Speech to Cursor

## 📋 Содержание

1. [Что это такое?](#что-это-такое)
2. [Требования](#требования)
3. [Установка](#установка)
4. [Настройка](#настройка)
5. [Использование](#использование)
6. [Решение проблем](#решение-проблем)

---

## Что это такое?

**Speech to Cursor** — это расширение для VS Code и Cursor, которое позволяет использовать голосовой ввод для работы с AI-ассистентом. Вместо того чтобы печатать, вы можете диктовать текст, который будет автоматически отправляться в чат Cursor AI.

### Основные возможности:

- 🎙️ **Голосовой ввод** — диктуйте текст вместо печати
- 🔄 **Несколько движков распознавания** — поддержка различных методов распознавания речи
- ⚙️ **Гибкая настройка** — множество опций конфигурации
- 📊 **Индикатор статуса** — визуальная индикация состояния записи

---

## Требования

### Минимальные требования:

- **VS Code или Cursor**: версия 1.75.0 или выше
- **Микрофон**: рабочий микрофон с предоставленными разрешениями
- **Браузер**: для режима браузерного резервирования (рекомендуется Chrome, Edge или Safari)

### Дополнительные требования (опционально):

- **macOS**: для поддержки нативной диктовки macOS
- **HTTPS**: некоторые браузеры требуют HTTPS для доступа к микрофону в production-окружении

---

## Установка

### Шаг 1: Установка расширения

1. Откройте **VS Code** или **Cursor**
2. Перейдите в раздел **Extensions** (Расширения):
   - Нажмите `Ctrl+Shift+X` (Windows/Linux) или `Cmd+Shift+X` (macOS)
   - Или через меню: **View** → **Extensions**
3. В поиске введите: `Speech to Cursor`
4. Найдите расширение и нажмите **Install** (Установить)

### Шаг 2: Проверка установки

После установки вы должны увидеть новый элемент в **Status Bar** (строке состояния) внизу окна:
- 🎤 **$(mic) Speech - Ready to record** — готово к записи

---

## Настройка

### Открытие настроек

1. Откройте настройки:
   - `Ctrl+,` (Windows/Linux) или `Cmd+,` (macOS)
   - Или через меню: **File** → **Preferences** → **Settings**
2. В поиске настроек введите: `speechToCursor`

### Доступные настройки

#### 1. **speechToCursor.engine** — Выбор движка распознавания речи

**Варианты:**
- `auto` (по умолчанию) — автоматический выбор: сначала пробует webview, затем браузер
- `webview` — панель VS Code
- `browser` — Chrome/Edge/Safari
- `os-dictation` — нативная диктовка macOS
- `api` — будущая поддержка API-распознавания

**Рекомендация:** Оставьте `auto` для автоматического выбора лучшего варианта.

#### 2. **speechToCursor.useBrowserFallback** — Всегда использовать браузер

- **По умолчанию:** `false`
- **Описание:** Если включено, всегда использует браузерный режим вместо webview

#### 3. **speechToCursor.useMacDictation** — Использовать диктовку macOS

- **По умолчанию:** `true` (только для macOS)
- **Описание:** Использует нативную диктовку macOS вместо Web Speech API

#### 4. **speechToCursor.language** — Язык распознавания

- **По умолчанию:** `en-US`
- **Примеры:**
  - `ru-RU` — русский язык
  - `en-US` — английский (США)
  - `tr-TR` — турецкий
  - `de-DE` — немецкий

**Важно:** Выберите язык, соответствующий языку вашей речи.

#### 5. **speechToCursor.interimResults** — Промежуточные результаты

- **По умолчанию:** `false`
- **Описание:** Показывать промежуточные результаты распознавания (может быть нестабильно)

#### 6. **speechToCursor.continuous** — Непрерывное распознавание

- **По умолчанию:** `true`
- **Описание:** Распознавание продолжается до остановки вручную

#### 7. **speechToCursor.browserAutoStart** — Автозапуск в браузере

- **По умолчанию:** `true`
- **Описание:** Автоматически начинать распознавание при открытии браузерной страницы

#### 8. **speechToCursor.outputMode** — Куда отправлять распознанный текст

**Варианты:**
- `chat` (по умолчанию) — в чат Cursor AI
- `editor` — в активный текстовый редактор
- `clipboard` — в системный буфер обмена

**Рекомендация:** Используйте `chat` для работы с Cursor AI.

### Пример конфигурации для русского языка

```json
{
  "speechToCursor.engine": "auto",
  "speechToCursor.language": "ru-RU",
  "speechToCursor.outputMode": "chat",
  "speechToCursor.continuous": true
}
```

---

## Использование

### Базовое использование

1. **Откройте чат Cursor AI:**
   - Нажмите `Ctrl+L` (Windows/Linux) или `Cmd+L` (macOS)
   - Или через меню: **View** → **Command Palette** → введите "Cursor Chat"

2. **Начните запись:**
   - Кликните на элемент статуса в **Status Bar**: `$(mic) Speech - Ready to record`
   - Или используйте команду: `Ctrl+Shift+P` → введите "Speech to Cursor: Start Recording"

3. **Говорите:**
   - Говорите четко и в нормальном темпе
   - Статус изменится на: `$(record) Listening…`

4. **Остановите запись:**
   - Кликните на статус снова
   - Или используйте команду: `Ctrl+Shift+P` → "Speech to Cursor: Stop Recording"

5. **Текст автоматически отправится:**
   - Если `outputMode` установлен в `chat`, текст появится в чате Cursor AI
   - Нажмите Enter для отправки или отредактируйте текст перед отправкой

### Работа с режимами вывода

#### Режим "chat" (по умолчанию)
- Распознанный текст появляется в поле ввода чата Cursor AI
- Вы можете отредактировать текст перед отправкой
- Нажмите Enter для отправки сообщения

#### Режим "editor"
- Текст вставляется в активный редактор в позицию курсора
- Полезно для диктовки кода или комментариев

#### Режим "clipboard"
- Текст копируется в буфер обмена
- Вставьте текст вручную (`Ctrl+V` / `Cmd+V`)

### Горячие клавиши (если настроены)

Вы можете настроить горячие клавиши для быстрого доступа:
1. `Ctrl+K Ctrl+S` (Windows/Linux) или `Cmd+K Cmd+S` (macOS) — открыть настройки клавиатуры
2. Найдите команды "Speech to Cursor"
3. Назначьте удобные комбинации клавиш

---

## Решение проблем

### Проблема: Микрофон не работает

#### Решение 1: Проверьте разрешения

**Windows:**
1. Откройте **Настройки** → **Конфиденциальность** → **Микрофон**
2. Убедитесь, что доступ к микрофону включен для приложений

**macOS:**
1. Откройте **Системные настройки** → **Безопасность и конфиденциальность** → **Микрофон**
2. Разрешите доступ для VS Code/Cursor

**Linux:**
- Убедитесь, что микрофон доступен системе
- Проверьте настройки PulseAudio или ALSA

#### Решение 2: Попробуйте браузерный режим

1. Откройте настройки расширения
2. Установите `speechToCursor.useBrowserFallback: true`
3. Перезапустите VS Code/Cursor

#### Решение 3: Проверьте микрофон в системе

- Убедитесь, что микрофон работает в других приложениях
- Проверьте уровень громкости микрофона

---

### Проблема: Текст не отправляется в Cursor

#### Решение 1: Проверьте режим вывода

1. Убедитесь, что `speechToCursor.outputMode` установлен в `chat`
2. Проверьте, что чат Cursor AI открыт

#### Решение 2: Используйте режим clipboard

1. Установите `speechToCursor.outputMode: "clipboard"`
2. После распознавания вставьте текст вручную (`Ctrl+V` / `Cmd+V`)

#### Решение 3: Перезапустите Cursor

- Закройте и снова откройте Cursor
- Проверьте, что расширение активно

---

### Проблема: Плохое качество распознавания

#### Решение 1: Проверьте язык

1. Убедитесь, что `speechToCursor.language` соответствует языку вашей речи
2. Для русского языка используйте: `ru-RU`

#### Решение 2: Улучшите условия записи

- Говорите в тихом помещении
- Говорите четко и в нормальном темпе
- Убедитесь, что микрофон находится на правильном расстоянии

#### Решение 3: Используйте macOS диктовку (только для Mac)

1. Установите `speechToCursor.useMacDictation: true`
2. Убедитесь, что диктовка включена в системных настройках macOS:
   - **Системные настройки** → **Клавиатура** → **Диктовка** → включите диктовку

#### Решение 4: Попробуйте другой движок

- Попробуйте `webview` вместо `browser` или наоборот
- На macOS попробуйте `os-dictation`

---

### Проблема: Браузерный режим требует взаимодействия пользователя

**Причина:** Браузеры требуют пользовательского взаимодействия для первого доступа к микрофону из соображений безопасности.

**Решение:**
1. При первом использовании браузерного режима кликните на кнопку разрешения доступа к микрофону
2. После первого разрешения проблема должна исчезнуть

---

### Проблема: Расширение не появляется в Status Bar

#### Решение 1: Проверьте установку

1. Убедитесь, что расширение установлено и активировано
2. Проверьте в разделе **Extensions** → **Installed**

#### Решение 2: Перезапустите редактор

- Закройте и снова откройте VS Code/Cursor

#### Решение 3: Проверьте логи

1. Откройте **Output** панель: `Ctrl+Shift+U` (Windows/Linux) или `Cmd+Shift+U` (macOS)
2. Выберите "Speech to Cursor" в выпадающем списке
3. Проверьте сообщения об ошибках

---

## Дополнительные советы

### Оптимизация качества распознавания

1. **Используйте качественный микрофон** — хороший микрофон значительно улучшает точность
2. **Говорите естественно** — не нужно говорить слишком медленно или быстро
3. **Делайте паузы** — паузы помогают системе лучше распознавать границы предложений
4. **Проверяйте текст** — всегда проверяйте распознанный текст перед отправкой

### Работа с кодом

- Для диктовки кода используйте режим `editor`
- Говорите названия функций и переменных четко
- Используйте команды типа "новая строка", "табуляция" если они поддерживаются

### Интеграция в рабочий процесс

- Используйте голосовой ввод для длинных комментариев и документации
- Комбинируйте голосовой ввод с клавиатурой для максимальной эффективности
- Настройте горячие клавиши для быстрого переключения режимов

---

## Часто задаваемые вопросы (FAQ)

### В: Поддерживается ли русский язык?

**О:** Да, установите `speechToCursor.language: "ru-RU"` в настройках.

### В: Можно ли использовать расширение без интернета?

**О:** Зависит от движка:
- `webview` и `browser` требуют интернет для Web Speech API
- `os-dictation` (macOS) работает офлайн

### В: Работает ли расширение в Linux?

**О:** Да, но может потребоваться дополнительная настройка разрешений для микрофона.

### В: Можно ли использовать несколько языков одновременно?

**О:** Нет, выберите один язык в настройках. Для смены языка измените настройку и перезапустите распознавание.

### В: Как отключить расширение временно?

**О:** Отключите расширение в разделе **Extensions** → найдите "Speech to Cursor" → нажмите **Disable**.

---

## Поддержка

Если у вас возникли проблемы, которые не описаны в этой инструкции:

1. Проверьте [официальный репозиторий расширения](https://github.com/your-repo/speech-to-cursor) (если доступен)
2. Откройте issue в репозитории проекта
3. Проверьте логи расширения в панели **Output**

---

## Заключение

Расширение **Speech to Cursor** — это мощный инструмент для повышения продуктивности работы с AI-ассистентом. Используйте голосовой ввод для более естественного взаимодействия с Cursor AI и ускорения рабочего процесса.

**Приятной работы с голосовым вводом! 🎤✨**


