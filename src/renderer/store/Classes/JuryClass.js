export default class JuryClass {
  constructor(title, lastName, name, loc) {
    this.title = title || "";
    this.name = name || "";
    this.lastName = lastName || "";
    this.loc = loc || "";
  }
}
